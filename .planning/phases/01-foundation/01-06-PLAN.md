---
phase: 01-foundation
plan: "06"
type: execute
wave: 5
depends_on:
  - "01-05"
files_modified: []
autonomous: false
requirements:
  - BRAND-01
  - BRAND-02
  - BRAND-03
  - PERF-01
  - PERF-02
  - PERF-03
  - ARTICLE-01
  - ARTICLE-02
  - ARTICLE-03

must_haves:
  truths:
    - "User completes the full pipeline: brand setup → CSV upload → topic input → article generation → copy to clipboard"
    - "Generated article passes brand voice spot check: uses practical how-to framing, references a specific Smash Balloon plugin feature, includes numbered steps"
    - "Generated article is SEO-appropriate: no keyword stuffing visible, meta description is accurate and under 160 chars"
  artifacts:
    - path: ".planning/phases/01-foundation/01-06-SUMMARY.md"
      provides: "Phase 1 completion record"
  key_links: []
---

<objective>
Human verification of the complete Phase 1 pipeline: brand voice setup, performance data upload, article generation with streaming, and the resulting article quality.

Purpose: Phase 1 proves the core value proposition. This checkpoint validates that a real Smash Balloon topic produces a publish-quality article draft — not just that the code runs.
Output: Confirmed pipeline completion and article quality sign-off before Phase 2 planning begins.
</objective>

<execution_context>
@/Users/liannelaroya/.claude/get-shit-done/workflows/execute-plan.md
@/Users/liannelaroya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: End-to-end pipeline quality verification</name>
  <files>none — human verification only</files>
  <action>
Verify the complete Phase 1 pipeline by running through all four sections of the app:
- /brand — 4-step brand voice wizard (brand basics, tone + style rules, example articles, plugin facts)
- /performance — GA4 CSV upload with parsed + classified performance table
- /generate — topic input, format picker (user-controlled), streaming article output (outline-first, then section-by-section)
- /drafts — saved draft list with copy and regenerate actions
  </action>
  <verify>
    <automated>MISSING — this plan is a human verification checkpoint. No automated command.</automated>
  </verify>
  <done>User types "approved" after confirming all pipeline steps and article quality checks pass.</done>
  <what-built>
Complete Phase 1 pipeline:
- /brand — 4-step brand voice wizard (brand basics, tone + style rules, example articles, plugin facts)
- /performance — GA4 CSV upload with parsed + classified performance table
- /generate — topic input, format picker (user-controlled), streaming article output (outline-first, then section-by-section)
- /drafts — saved draft list with copy and regenerate actions
  </what-built>
  <how-to-verify>
**Step 1 — Brand setup (5 min)**
1. Visit http://localhost:3000/brand
2. Complete the 4-step wizard:
   - Brand Basics: name = "Smash Balloon", audience = "WordPress site owners displaying social feeds"
   - Tone & Style: add at least 2 structural rules (e.g., "Always open with reader's specific problem", "Use numbered steps for sequences of 3+ steps")
   - Example Articles: paste the first 3-4 paragraphs of a real Smash Balloon article into Article 1 textarea
   - Plugin Facts: add one plugin entry (e.g., name: "Social Feed Pro", features: "Instagram feed display, auto-refresh", pricing: "$49/year")
3. Click Save
4. Verify: /brand shows profile summary with brand name and edit link

**Step 2 — Performance data (2 min)**
1. Visit http://localhost:3000/performance
2. Upload a GA4 CSV (use a real export or the test fixture at tests/csv/fixtures/ga4-sample.csv)
3. Verify: table shows Article title | Sessions | Format type, ranked by sessions
4. Verify: format type column shows values (how-to, list, comparison, or other)

**Step 3 — Article generation (10-15 min)**
1. Visit http://localhost:3000/generate
2. Enter a real Smash Balloon topic, e.g.: "How to Display Your Instagram Feed on Your WordPress Homepage"
3. Format picker: select "Use top-performing format" if available, or choose "How-To Guide" manually
4. Click "Generate Article"
5. Verify streaming UX: outline headings appear immediately, then body content fills in section by section, top to bottom, word-by-word

**Step 4 — Article quality check**
Review the completed article for:
- [ ] H1 title includes the target keyword naturally
- [ ] At least 4 H2 sections with specific (not generic) headings
- [ ] Article references a specific Smash Balloon plugin feature from the facts sheet (not invented)
- [ ] Numbered steps appear somewhere in the body (practical how-to format)
- [ ] No keyword stuffing — the target keyword appears naturally, not crammed
- [ ] Meta description shown is under 160 chars and accurately describes the article
- [ ] Conclusion includes a plugin CTA

**Step 5 — Copy and save**
1. Click "Copy article" — paste into a text editor and verify markdown heading levels (# for H1, ## for H2, etc.)
2. Visit http://localhost:3000/drafts — verify the draft appears with "Draft Ready" status
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (brand voice, streaming UX, article quality, missing features)</resume-signal>
</task>

</tasks>

<verification>
All verification is in the checkpoint task above.
</verification>

<success_criteria>
- User confirms full pipeline works end-to-end (brand setup → CSV → generate → copy)
- Article streaming UX is word-by-word with outline structure visible immediately
- Generated article references real plugin facts (no hallucinated features)
- Article quality meets Phase 1 success criteria from ROADMAP.md:
  - Practical how-to format with numbered steps
  - Brand voice: conversational but professional
  - SEO: target keyword in title, meta description under 160 chars, no stuffing
  - Plugin CTA in conclusion
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md` using the summary template.

Update ROADMAP.md Phase 1 status to completed.
Update STATE.md: status → "Phase 1 complete", current focus → Phase 2.
</output>
